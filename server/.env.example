# CORS Configuration
# Set this to your exposed origin URL for production
# Example: CORS_ORIGIN=https://poule.ricjuh.nl
# Leave as '*' to allow all origins (less secure but works for testing)
CORS_ORIGIN=*

# Server port (optional, defaults to 3000)
# PORT=3000
