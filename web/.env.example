# API URL for development (container-to-container communication)
# When running in Docker, use the service name 'server' instead of localhost
# For local development without Docker, change to http://localhost:3000/api
VITE_API_URL=http://server:3000/api
